---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// Fetch all tools from Content Collection
const allTools = await getCollection('tools');

const toolCategories = [
  { slug: 'all', label: 'Tất cả', icon: '🔧' },
  { slug: 'extensions', label: 'Extensions', icon: '🔌' },
  { slug: 'crm', label: 'CRM', icon: '💼' },
  { slug: 'seo', label: 'SEO', icon: '🔍' },
  { slug: 'productivity', label: 'Productivity', icon: '📊' },
];
---

<BaseLayout
  title="Tools & Extensions"
  description="Công cụ AI và extension giúp bạn làm việc hiệu quả hơn. Từ quản lý task đến CRM và SEO."
>
  <section class="py-16 lg:py-24 bg-white dark:bg-charcoal">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <div class="text-center mb-12">
        <span
          class="inline-block px-3 py-1 bg-blue-500/10 text-blue-500 text-xs font-semibold rounded-full mb-4"
          >TOOLS & EXTENSIONS</span
        >
        <h1 class="text-3xl lg:text-4xl font-bold mb-4">Công cụ giúp bạn làm việc hiệu quả</h1>
        <p class="text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
          Extension, CRM và công cụ SEO được phát triển bởi Bình Vương để hỗ trợ doanh nghiệp Việt
          Nam.
        </p>
      </div>

      <!-- Category Filter -->
      <div class="flex flex-wrap justify-center gap-2 mb-12">
        {
          toolCategories.map((cat, index) => (
            <button
              class={`px-4 py-2 text-sm font-medium rounded-full transition-colors ${
                index === 0
                  ? 'bg-blue-500 text-white'
                  : 'bg-gray-100 dark:bg-white/5 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-white/10'
              }`}
            >
              {cat.icon} {cat.label}
            </button>
          ))
        }
      </div>

      <!-- Tools Grid -->
      {allTools.length > 0 ? (
        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {
            allTools.map((tool) => {
              const toolUrl = `/tools/${tool.slug}/`;
              return (
                <article class="group bg-gray-50 dark:bg-charcoal-light/30 border border-gray-200 dark:border-white/5 rounded-2xl overflow-hidden hover:border-blue-500/30 hover:shadow-xl transition-all">
                  <a href={toolUrl} class="block">
                    <div class="aspect-[16/9] relative overflow-hidden">
                      <img
                        src={tool.data.image}
                        alt={tool.data.title}
                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                        loading="lazy"
                      />
                      <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent" />
                      <span
                        class={`absolute top-4 left-4 px-2 py-1 text-xs font-semibold rounded ${tool.data.pricing.isFree ? 'bg-green-500 text-white' : 'bg-purple-500 text-white'}`}
                      >
                        {tool.data.pricing.isFree ? 'FREE' : 'PRO'}
                      </span>
                    </div>
                  </a>
                  <div class="p-6">
                    <h3 class="text-lg font-bold mb-2 group-hover:text-blue-500 transition-colors">
                      <a href={toolUrl}>{tool.data.title}</a>
                    </h3>
                    <p class="text-sm text-gray-600 dark:text-gray-400 mb-4 line-clamp-2">
                      {tool.data.description}
                    </p>
                    <div class="flex items-center gap-3 text-sm text-gray-500 mb-4">
                      {tool.data.users && <span>👥 {tool.data.users}</span>}
                      {tool.data.rating && (
                        <span>
                          ⭐ {tool.data.rating} ({tool.data.reviewCount})
                        </span>
                      )}
                    </div>
                    <div class="flex items-center justify-between">
                      <span class="text-xs text-gray-400 capitalize">{tool.data.category}</span>
                      <a href={toolUrl} class="text-bv-red font-semibold text-sm hover:underline">
                        {tool.data.pricing.isFree ? 'Dùng ngay →' : `${tool.data.pricing.price} →`}
                      </a>
                    </div>
                  </div>
                </article>
              );
            })
          }
        </div>
      ) : (
        <div class="text-center py-12">
          <p class="text-gray-500 dark:text-gray-400">Đang cập nhật công cụ...</p>
        </div>
      )}
    </div>
  </section>
</BaseLayout>
